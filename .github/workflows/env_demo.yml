name: Environment Demo
on: workflow_dispatch
env:
  APP_ENV: "test"
jobs:
  env_demo:
    env:
      APP_ENV: "test"
    runs-on: ubuntu-latest
    steps:
      - name: Connecting to the repo
        run: echo "App is connected to the $APP_ENV"
